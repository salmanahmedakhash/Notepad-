// FIX: Import Dispatch and SetStateAction types to resolve type errors in the function signature below.
import { useState, useEffect, Dispatch, SetStateAction } from 'react';

// FIX: Update function signature to use imported types `Dispatch` and `SetStateAction` instead of `React.Dispatch` and `React.SetStateAction`. Also removed trailing comma from generic parameter.
export function useLocalStorage<T>(key: string, initialValue: T): [T, Dispatch<SetStateAction<T>>] {
  const [storedValue, setStoredValue] = useState<T>(() => {
    try {
      const item = window.localStorage.getItem(key);
      return item ? JSON.parse(item) : initialValue;
    } catch (error) {
      console.error(error);
      return initialValue;
    }
  });

  useEffect(() => {
    try {
      const valueToStore =
        typeof storedValue === 'function'
          ? storedValue(storedValue)
          : storedValue;
      window.localStorage.setItem(key, JSON.stringify(valueToStore));
    } catch (error) {
      console.error(error);
    }
  }, [key, storedValue]);

  return [storedValue, setStoredValue];
}
